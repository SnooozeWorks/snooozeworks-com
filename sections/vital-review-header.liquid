{%- if section.settings.show_section -%}

  <section class="Section Section--spacingNormal" data-section-id="{{ section.id }}" data-section-type="vital-review-header" data-section-settings='{{ section_settings }}'>
    {%- if section.settings.heading != blank -%}
      <header class="SectionHeader SectionHeader--custom">
          <h2 class="SectionHeader__Heading Heading u-h2">{{ section.settings.heading | escape }}</h2>
      </header>
    {%- endif -%}

    <div id="bundle-product_reviews"></div>
    
  </section>

{%- endif -%}


{% schema %}
  {
    "name": "Reviews",
    "settings": [
    {
      "type": "checkbox",
      "id": "show_section",
      "label": "Show section",
      "default": true
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Customer Reviews"
    }]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}